# Contract §15 — duplicate of repo-root `.env.example` for the required `config/` path.
# Prefer copying from the repository root:
#   cp .env.example .env.local
# Or from this file (identical content).

DATABASE_URL="postgresql://ii:ii@localhost:5434/instructional_integrity"
NODE_ENV="development"
LOG_LEVEL="info"
MAX_UPLOAD_SIZE_BYTES="5242880"
MAX_PASTE_LENGTH="100000"
EVALUATION_TIMEOUT_MS="30000"
MAX_CONCURRENT_EVALUATIONS="5"
# CORS_ALLOWED_ORIGINS="https://app.example.com"
