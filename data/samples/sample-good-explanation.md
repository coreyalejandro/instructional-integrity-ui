# Good scaffolding example

## Before you start
Assumes you can open a terminal and have Node 18+ installed.

## Steps
1. Clone the repository.
2. Run `npm install`.
3. Run `npm test` because the README documents expected behavior.

## Evidence
Because each step depends only on the previous one, the learner can follow without hidden prerequisites.
