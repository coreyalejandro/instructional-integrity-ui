services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ii
      POSTGRES_PASSWORD: ii
      POSTGRES_DB: instructional_integrity
    ports:
      - "5434:5432"
    volumes:
      - ii_pg_data:/var/lib/postgresql/data

volumes:
  ii_pg_data:
