export type Verdict = "pass" | "warn" | "fail" | "needs_human_review";

export type CriterionVerdict = "pass" | "warn" | "fail";

/** UI + API label for run-level verdict */
export type VerdictLabel = Verdict;
